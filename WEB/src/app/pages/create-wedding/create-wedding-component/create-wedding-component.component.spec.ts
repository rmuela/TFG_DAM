/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateWeddingComponentComponent } from './create-wedding-component.component';

describe('CreateWeddingComponentComponent', () => {
  let component: CreateWeddingComponentComponent;
  let fixture: ComponentFixture<CreateWeddingComponentComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CreateWeddingComponentComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CreateWeddingComponentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/